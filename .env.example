# Server port. Defaults to 3000.
PORT=3000

# Runtime data directory. Defaults to ./data.
# Use a temp path for experiments or tests.
DATA_DIR=./data

# Production mode serves the built Vite client from dist/client.
NODE_ENV=production
